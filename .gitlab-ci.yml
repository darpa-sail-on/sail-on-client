image: pytorch/pytorch:latest

.python-setup: &python-setup
  - pip install pipenv
  - pipenv install -d

lint:
  tags:
    - linux
    - docker
    - build
  script:
    - *python-setup
    - pipenv run lint
